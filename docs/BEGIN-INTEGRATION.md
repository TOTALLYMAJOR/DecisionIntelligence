# Begin: advisory participation in governed integration

Status: PREPARED_AWAITING_BEGIN
Prepared: 2026-09-18.
Scope: current work-selection instructions, not a new task runtime or human approval.

## Selected program

The shared work directive is docs/BEGIN-INTEGRATION.md in TOTALLYMAJOR/PROOFLOOM on branch codex/proofloom-trust-repair-20260917. Resolve the current document and local AGENTS.md before acting on a later explicit operator `begin` for this integration. That word is a natural-language continuation instruction, not a CLI command, scheduler trigger or authenticated approval receipt. This preparation starts no implementation.

The selected objective is a local Proofloom -> AgentFlow -> Traffic Control -> Proofloom execution/evidence loop. AgentFlow already owns execution and multi-repository coordination. DecisionIntelligence, whose product is named Limitless Architecting OS, supplies advisory architecture-impact analysis and draft decision contracts. It does not become another executor, Governor, approval authority or canonical design-memory store.

## First milestone

DecisionIntelligence is not on the critical path of an ordinary successful first local integration run. On begin in this repository, inspect/read the shared status and participate read-only; do not automatically run the starter prompt sequence, rebuild the UI, wire the entire database/corpus, turn on live providers or launch unrelated Docker work merely because a fixture-backed surface remains incomplete.

Only a real, evidence-bound structural reconsideration from the selected integration opens a new bounded advisory task here. Its exact scope must be selected and authorized through local repository rules before code changes. If no such reconsideration exists, report that this repository has no implementation action for the current milestone and continue coordination in the owning repositories when tools and permissions permit; otherwise identify that owner without inventing work.

## When structural reconsideration is actually required

Reuse packages/core/src/decision.ts, the existing DecisionStudio/compiler interfaces, repository-snapshot boundaries and evidence model. Accept the originating handoff/decision references as untrusted inputs until verified by their owning systems. Produce distinguishable alternatives, constraints, supporting evidence, unknowns, falsifiers and required human decisions. Keep outputs draft/advisory. A draft contract, impact score or model statement cannot approve execution or a replacement handoff.

Return candidate alternatives to the existing Proofloom structural-escalation path. Human selection and a newly approved native handoff remain separate. Do not duplicate Approval, Governor, scheduler, database, retry or cross-repository execution services here. Preserve the distinction between static repository snapshots, historical evidence, inferred impact and runtime proof.

## Boundaries and validation

Keep AGENTS.md's truth hierarchy, domain ownership, Git authority and non-negotiable rules. Do not treat a fixture demonstration as durable backend wiring, a provider call, product adoption or an observed user outcome. Do not claim an implementation or check ran from reading documentation.

For any later approved code change, use the exact local write set and existing npm validation commands. No commits, pushes, merges, deployment, secret/provider changes or destructive Docker operations without the explicit authority required by AGENTS.md. The current request authorizes this instruction preparation only, not those future implementation actions. Missing access, evidence or approval is a real blocker; preserve it rather than fabricating authority.

The exported V7-EPI proposal packages are reference material, not an instruction to build a new cognitive OS or replace the native Proofloom/AgentFlow contracts. Do not reopen product architecture solely because the operator used the prepared start word.
