import { failures } from '@limitless/core';
import { EvidenceBadge } from '../../components/EvidenceBadge';
import { PageHeader } from '../../components/PageHeader';

export default function FailureLabPage(){return <><PageHeader eyebrow="Compound engineering capital" title="Failure Lab" description="Verified mistakes become root causes, corrections, prevention controls, tests, and reusable architecting modules."/><div className="failureGrid">{failures.map(failure=><article className="failureCard" key={failure.failureId}><header className="failureTitle"><div><span className="entityMeta">{failure.failureId} · {failure.observedIn}</span><h2 style={{marginTop:7}}>{failure.title}</h2></div><EvidenceBadge grade={failure.evidenceGrade}/></header><div className="failureFlow"><div className="failureStage"><span>Trigger</span><strong>What happened</strong><p>{failure.trigger}</p></div><div className="failureStage"><span>Root cause</span><strong>Why it happened</strong><p>{failure.rootCause}</p></div><div className="failureStage"><span>Correction</span><strong>What changed</strong><p>{failure.correction}</p></div><div className="failureStage"><span>Control</span><strong>{failure.generalizedRule}</strong><p>{failure.control}</p></div></div></article>)}</div></>}
