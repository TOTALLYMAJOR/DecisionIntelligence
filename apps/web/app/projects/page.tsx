import { projects } from '@limitless/core';
import { PageHeader } from '../../components/PageHeader';
export default function ProjectsPage(){return <><PageHeader eyebrow="Cross-project intelligence" title="Projects" description="Each repository contributes evidence to the methodology while retaining its own product and authority boundaries."/><div className="entityGrid">{projects.map(project=><article className="entityCard" key={project.id}><div className="entityCardTop"><span className="entityMeta">project</span><span className="tag">{project.status}</span></div><h3>{project.name}</h3><p>{project.role}</p><span className="entityLink">Evidence-producing subject</span></article>)}</div><div className="callout" style={{marginTop:16}}>QuietPilot and QuotePilot remain source projects. Limitless Architecting OS is the cross-project intellectual-property system; it does not share their business tables or mutate their production behavior.</div></>}
