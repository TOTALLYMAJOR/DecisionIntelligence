import { profileClaims } from '@limitless/core';
import { EvidenceBadge } from '../../components/EvidenceBadge';
import { PageHeader } from '../../components/PageHeader';
import { formatPercent, titleCase } from '../../lib/format';

export default function ProfilePage(){return <><PageHeader eyebrow="Evidence-grounded creator model" title="Major Mike" description="A professional architecture profile built from reviewed behavior and source evidence—not clinical inference, generic praise, or claims of manually writing every agent-generated line."/><div className="profileHero"><aside className="panel profileIdentity"><div className="profileAvatar">MM</div><h2>Founder & Principal Product Systems Architect</h2><p>Proof-governed service commerce, multi-tenant SaaS, operational intelligence, and AI-assisted product engineering.</p><div className="callout" style={{textAlign:'left',marginTop:18}}>Authorship boundary: product thesis, questions, architecture priorities, standards, and final decisions are separated from agent-assisted code, tests, documentation, and visual implementation.</div></aside><section className="panel"><div className="panelHeader"><div><h2>Reviewed profile claims</h2><p>Every claim exposes confidence and an alternative explanation.</p></div></div><div className="panelBody"><div className="claimList">{profileClaims.map(claim=><article className="claimRow" key={claim.claimId}><div><span className="entityMeta">{claim.claimId} · {titleCase(claim.category)}</span><p style={{marginTop:7}}>{claim.claim}</p><p className="claimAlternative"><strong>Alternative:</strong> {claim.alternativeExplanation}</p></div><div className="claimMeta"><EvidenceBadge grade={claim.evidenceGrade}/><span className="mono">{formatPercent(claim.confidence)}</span></div></article>)}</div></div></section></div></>}
