import { PageHeader } from '../../components/PageHeader';
const stages=[['Vision','What category or outcome should exist?'],['Probe','Ask the shortest question that exposes uncertainty.'],['Contradiction','Locate where claim, architecture, and runtime diverge.'],['Authority','Name who or what may establish each state.'],['Evidence','Declare what durable proof makes the claim true.'],['Dependency','Map the blast radius and stale downstream obligations.'],['Bounded implementation','Make the smallest complete and attributable change.'],['Adversarial test','Attack false positives, ambiguity, tenancy, retries, and wiring.'],['Repair','Correct the root cause without widening the slice.'],['Receipt','Record whether the exact operation committed.'],['Reusable pattern','Promote the lesson into the constitution and compiler.']];
export default function MethodologyPage(){return <><PageHeader eyebrow="Proof-Governed Product Architecture" title="Trial-to-truth architecting" description="A repeatable method for turning uncertainty into governed implementation and implementation failure into reusable control."/><section className="panel"><div className="panelBody"><div className="timeline">{stages.map(([title,detail],index)=><div className="timelineItem" key={title}><div className="timelineRail"><span className="timelineDot"/></div><div className="timelineContent"><span>{String(index+1).padStart(2,'0')}</span><h3>{title}</h3><p>{detail}</p></div></div>)}</div></div></section></>}
