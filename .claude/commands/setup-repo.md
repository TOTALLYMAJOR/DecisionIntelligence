Read `AGENTS.md`, `CLAUDE.md`, and `docs/prompts/00_SETUP_REPOSITORY.md`. Execute the repository
setup prompt from the verified repository root. Do not print secrets, push, deploy, or modify
provider configuration. End with the exact commands run, results, and next eligible task.
