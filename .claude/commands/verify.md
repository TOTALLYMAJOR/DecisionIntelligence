Read `AGENTS.md`. Run the narrowest relevant tests for the current diff, then execute
`npm run verify`. Report PASS, FAIL, or BLOCK with exact failing commands and no unsupported
claims. Do not patch unrelated failures.
